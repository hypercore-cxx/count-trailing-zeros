#ifndef HYPERCORE_COUNT_TRAILING_ZEROS
#define HYPERCORE_COUNT_TRAILING_ZEROS

#include <stddef.h>

namespace Hyper {
  namespace Core {
    size_t ctz (size_t v);
  } // namespace Core
} // namespace Hyper

#endif
